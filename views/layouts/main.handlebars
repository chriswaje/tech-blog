<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Talk Techy</title>
</head>

<body>
    {{!-- Navbar for application --}}
    <nav class='navbar'>
        <ul class='flex-row'>
            <li class='li-inline nav-item'><a href='/'>Home</a></li>
            <li class='li-inline nav-item'><a href='/dashboard'>Dashboard</a></li>
            {{#if loggedIn}}
            <li class='li-inline nav-item'><a href='#' id='logout'>Logout</a></li>
            {{else}}
            <li class='li-inline nav-item'><a href='/login'>Login</a></li>
            {{/if}}
        </ul>
    </nav>

    <header class='hero'>
        {{#if dashboard}}
        <h1 class='app-title'><a href='/dashboard'>Dashboard</a></h1>
        {{else}}
        <h1 class='app-title'><a href='/'>Talk Techy</a></h1>
        {{/if}}
    </header>

    <main class='col-auto'>
        {{{body}}}
    </main>

    {{#if dashboard}}
    <footer class='footer flex-row justify-center'>
        <a href='/dashboard/new'>Create Post</a>
    </footer>
    {{/if}}
</body>

</html>